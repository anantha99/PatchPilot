{
  "name": "multifile-entitlement-ledger",
  "suite": "v2-multifile",
  "bug_shape": "webhook normalization, entitlement policy, ledger delta, and idempotency drift",
  "goal": "Repair subscription webhook processing so events are normalized, entitlement access is correct, seat ledger deltas are accurate, and duplicate events are idempotent.",
  "test_command": "pytest",
  "expected_changed_source_files": [
    "entitlements/events.py",
    "entitlements/policy.py",
    "entitlements/ledger.py",
    "entitlements/processor.py"
  ],
  "allowed_changed_files": [
    "entitlements/events.py",
    "entitlements/policy.py",
    "entitlements/ledger.py",
    "entitlements/processor.py"
  ],
  "minimum_changed_files": 4,
  "expected_validation_commands": ["pytest tests/test_webhook_processor.py", "pytest"],
  "flagship": true
}
